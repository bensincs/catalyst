# Values that let `helm lint` and `helm template` run in CI, where none of the
# platform's wiring exists. Nothing here is used at deploy time.
image:
  repository: example.invalid/agentgateway
  tag: v1.4.1

# CI has no platform wiring, so the database host is never filled in.
database:
  enabled: false
replicas: 1
